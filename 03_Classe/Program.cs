namespace _03_Classe
{
    class Program
    {
        static void Main(string[] args)
        {
            // Instanciation => Création d'une instance de l'objet => Création d'une référence en mémoire
            // DemoClass = type complexe
            // Type référence => Stockage de l'adresse en mémoire dans la variable qui sera de type DemoClass

            DemoClass demo = new DemoClass();
        }
    }
}